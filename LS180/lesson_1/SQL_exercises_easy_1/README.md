These exercises are executed in psql console. For client applications (represented as comments in the sql file(s)),
run them as command lines in the terminal.

# Run 'createdb animals'
# Run 'psql animals < animals.sql'